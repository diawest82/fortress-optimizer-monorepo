[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "fortress-shared-libs"
version = "1.0.0"
description = "Shared libraries for Fortress Token Optimizer"
